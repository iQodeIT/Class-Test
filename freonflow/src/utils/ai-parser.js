/**
 * Utility for parsing unstructured job descriptions using Gemini AI.
 * Placeholder for future integration.
 */

export const parseJobDescription = async (text) => {
  console.log("Parsing job description with AI placeholder:", text);

  // This will eventually call the Gemini API
  // Example response structure:
  return [
    { description: "Service Call / Diagnostic", quantity: 1, unit_price: 85.00 },
    { description: "R-410A Refrigerant (per lb)", quantity: 2, unit_price: 120.00 }
  ];
};
